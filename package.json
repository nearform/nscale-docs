{
  "name": "nscale-docs",
  "version": "1.0.0",
  "description": "The doc-site for nscale",
  "main": "build.js",
  "license": "MIT",
  "private": "true",
  "scripts": {
    "build": "node build.js",
    "serve": "node build.js -s",
    "deploy": "node build.js && surge --project ./dist/ --domain nscale.surge.sh"
  },
  "dependencies": {
    "handlebars": "^3.0.3",
    "metalsmith": "^1.7.0",
    "metalsmith-ignore": "^0.1.2",
    "metalsmith-layouts": "^1.0.0",
    "metalsmith-markdown": "^0.2.1",
    "metalsmith-move-up": "1.0.0",
    "metalsmith-register-partials": "^1.0.2",
    "metalsmith-serve": "0.0.3",
    "metalsmith-watch": "^0.2.0",
    "minimist": "^1.1.1"
  }
}
